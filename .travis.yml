language: python

before_install:
 - pip install --upgrade pip setuptools
 - pip install planemo

script:
 - planemo lint ${TRAVIS_BUILD_DIR}/tools/dimspy
 - planemo test --conda_auto_init --conda_auto_install --conda_ensure_channels computational-metabolomics,iuc,bioconda,conda-forge,defaults ${TRAVIS_BUILD_DIR}/tools/dimspy
 
