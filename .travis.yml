language: python

before_install:
 - pip install --upgrade pip setuptools
 - pip install planemo

script:
 - planemo lint ${TRAVIS_BUILD_DIR}/tools/dimspy
 - planemo test --galaxy_branch release_19.01 --conda_ensure_channels computational-metabolomics,iuc,bioconda,conda-forge,defaults ${TRAVIS_BUILD_DIR}/tools/dimspy
